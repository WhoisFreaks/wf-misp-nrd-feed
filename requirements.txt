requests>=2.28.0

# PyMISP is NOT required at runtime -- this tool writes the feed format
# directly and never calls the MISP API. It is only needed to run the
# format-equivalence tests in tests/.
#   pip install pymisp